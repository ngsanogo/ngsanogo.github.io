# Optional overrides for the development server (src/dev.py).
# Copy to .env and adjust if needed; the app falls back to config.py defaults.

# Host to bind (use 0.0.0.0 when running inside Docker to accept external connections)
DEV_SERVER_HOST=localhost

# Port for the local preview server
DEV_SERVER_PORT=8000
